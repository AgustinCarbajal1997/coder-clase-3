import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titleContainer: {
    height: 30,
  },
  titleText: {
    color: "black",
    fontSize: 20,
    fontWeight: "600",
  },
});

export default styles;
